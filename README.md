# arvhug.github.io

Personal site for Arvind (Arv) Hughes — https://arvhug.github.io

Static HTML, no build step. `index.html` is the landing page; `about.html`,
`projects.html`, `cv.html` and `quasars.html` share `styles.css`.

`fermle/` and `Set_Times_Optimiser/` are standalone side projects served from
the same domain.
